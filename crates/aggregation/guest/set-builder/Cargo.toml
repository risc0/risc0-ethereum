[package]
name = "set-builder"
version = "0.4.0-rc.1"
edition = "2021"

[workspace]

[dependencies]
risc0-aggregation = { version = "0.4.0-rc.1", path = "../..", default-features = false }
risc0-zkvm = { version = "2.0.0-rc.2", default-features = false, features = ["std"] }
