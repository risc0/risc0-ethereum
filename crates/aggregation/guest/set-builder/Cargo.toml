[package]
name = "set-builder"
version = "0.8.0"
edition = "2021"

[workspace]

[dependencies]
risc0-aggregation = { version = "0.8.0", path = "../..", default-features = false }
risc0-zkvm = { git = "https://github.com/risc0/risc0", branch = "release-2.3", default-features = false, features = ["std"] }
