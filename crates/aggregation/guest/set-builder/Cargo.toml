[package]
name = "set-builder"
version = "0.9.0"
edition = "2021"

[workspace]

[dependencies]
risc0-aggregation = { version = "0.9.0", path = "../..", default-features = false }
risc0-zkvm = { version = "3.0.3", default-features = false, features = ["std"] }
