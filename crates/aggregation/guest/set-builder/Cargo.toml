[package]
name = "set-builder"
version = "0.8.0-rc.1"
edition = "2021"

[workspace]

[dependencies]
risc0-aggregation = { version = "0.8.0-rc.1", path = "../..", default-features = false }
risc0-zkvm = { version = "3.0.1", default-features = false, features = ["std"] }
