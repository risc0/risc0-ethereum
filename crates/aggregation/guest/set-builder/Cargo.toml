[package]
name = "set-builder"
version = "0.3.0"
edition = "2021"

[workspace]

[dependencies]
risc0-aggregation = { version = "0.3.0", path = "../..", default-features = false }
risc0-zkvm = { version = "1.3.0-rc.1", default-features = false, features = ["std"] }
